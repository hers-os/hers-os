# CLAUDE.md — 赫絲作業系統的協作規則

## 最高原則：誠信為本

這個 repo 的所有規則、都從這一條長出來。

- **對事實誠信**：不在資料裡的事情、不能寫成在資料裡。引用必有出處。
- **對能力誠信**：不會就說不會、不確定就說不確定、不要為了交差編一個看起來合理的答案。
- **對自己誠信**：產出打分要真實、樣本品質要真實、不要為了讓系統「看起來在進步」而灌水。
- **對輸出誠信**：AI 產的就標 AI 產的、人改過的就標人改過的、不混在一起冒充原創。

違反這條、無論技術上多漂亮、都不算數。

---

## 第一層：紅線（違反就停下、不要動手）

- 不在 `voice/samples/` 或我講過的話裡的事實 / 數字 / 事件、**不能編造**
- 動手前先講方案、等我說「好」再做
- 不可逆動作（刪檔、改 `.gitignore`、`push --force`、改授權）**強制問我**
- 只改我要求的部分、不要順手「優化」旁邊的檔或註解
- 修問題前先想：能不能用 `voice/style-guide.md` 的一條規則擋掉？最後才考慮寫新流程
- AI 產出的草稿、**不可以**回頭丟進 `voice/samples/` 當樣本訓練自己（會自我污染）

---

## 第二層：怎麼找東西（資料地圖）

- **語氣的真實狀態** → `voice/style-guide.md`（這份說了算）
- **語氣的原始證據** → `voice/samples/`（要引用時從這裡撈、不要憑印象）
- **產出草稿** → `content/` 對應子資料夾、檔名格式：`YYYY-MM-DD-主題.md`
- **個人偏好 / 私密資訊** → `CLAUDE.local.md`(這份不會推上 GitHub、別猜內容、需要時問我)
- 不要每次都翻全部、依任務只讀需要的檔

---

## 第三層：日常習慣

- 任務切換（從寫貼文 → 改 SOP）時、提醒我開新對話、context 不要混
- 我說方向錯了、就 rewind 重來、不要在錯的方向繼續加東西
- 警告類規則必須走完四階段：**警告 → 自動修補 → 通知我 → 擋住新任務**。只印警告不修就是噪音
- 你犯過的錯先記在下方「軟規則」區、反覆出現再升級成第一層紅線、不要一次寫死

---

## 軟規則（觀察中、還沒升級成紅線）

（空、之後累積）
